<div class="container margin-top">
  <div class="row color-border">
    {{!-- con #each recorremos cada elemento de movies y debajo con src la pedimos las imagenes del objeto. --}}
    {{#each movies}}  
    <div class="col-md-4">
      <div class="element-card">
        <div class="img-box">
          <img class="img-fluid" src="{{this.image}}" />
        </div>
        <div class="element-caption text-center">
    {{!-- aqui hacemos lo mismo con el titulo que metemos en h4 --}}
          <h4 class="text-center">{{this.title}}</h4>
    {{!-- aqui creamos un enlace y en la ruta de este, dandole la var id, creamos directamente la asociación con el id de las colecciones --}}
        <a href="/movies/{{id}}" class="btn btn-info">See More</a>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
</div>